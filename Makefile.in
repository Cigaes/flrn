DESTDIR=

all:
	+ cd src && ${MAKE} all
	+ cd po && ${MAKE} all

clean:
	+ cd src && ${MAKE} clean
	+ cd po && ${MAKE} clean

flrn:
	+ cd src && ${MAKE} flrn

depend:
	+ cd src && ${MAKE} depend

install:
	+ cd src && ${MAKE} install
	+ cd po && ${MAKE} install
